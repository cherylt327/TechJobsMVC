<h1>@ViewBag.title</h1>

<h3>@ViewBag.jobs.Count Result(s)</h3>

 
        @foreach (Dictionary<string, string> jobDict in ViewBag.jobs)
    {
        <div>
        <table class="job-listing">
        @foreach (KeyValuePair<string, string> job in jobDict)
        {
                <tr>
                    <td>@job.Key</td>
                    <td>@job.Value</td>
                </tr>
        }
            </table>
            </div>

    }
}